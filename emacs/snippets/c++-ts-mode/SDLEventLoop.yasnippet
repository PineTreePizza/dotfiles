# -*- mode: snippet -*-
# name: SDLEventLoop
# key: SDLEventLoop
# --
bool quit = false;
while (!quit) {
  SDL_Event e;
  while (SDL_PollEvent(&e) != 0) {
    if (e.type == SDL_QUIT) {
      quit = true;
    }
  }
}